#include "test_runner.h"
#include "profile.h"

template<typename GT>
map <GT, vector<GT>> GenereateRandomGraph(GT Edge, GT Vertex) {
    return {1, {1, 2, 3}};
}

int main() {
    map <int, vector<int>> test = GenereateRandomGraph(1, 3);
}
